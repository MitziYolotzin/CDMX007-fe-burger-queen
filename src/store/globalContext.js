import { createContext } from 'react';

const globalContext = createContext();
export default globalContext;

//Create component parent, management state global for all components
//Create context object for global state